git clone git://github.com/tkmtmkt/dotfiles.git

for f in $(ls _*)
do
  dotF=$(echo $f | sed 's/^_/\./')
  f1="$PWD/dotfiles/$f"
  f2="~/$dotF"

  echo "$f1 => $f2"
  [ -f $f2 ] && mv $f2 $f2.org
  ln -s $f1 $f2
done

