# .bash_profile

alias lz='ls -Z'

alias memo='vim ~/work/`date +"%Y/%m/%d/%Y%m%d.mkd"`'

alias today='cd ~/work/`date +"%Y/%m/%d"`'

alias fos='fossil'

alias sbt='sbt -ivy ~/.sbt/repository'

# vim: set ft=sh :
